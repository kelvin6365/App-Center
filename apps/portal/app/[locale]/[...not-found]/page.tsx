import NotFound from '@/app/[locale]/[...not-found]/not-found';
import React from 'react';

const Page = () => {
  return (
    <div>
      NotFound Page
      <NotFound />
    </div>
  );
};

export default Page;
